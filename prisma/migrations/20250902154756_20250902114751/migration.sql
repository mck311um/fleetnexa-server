/*
  Warnings:

  - You are about to drop the column `primaryDriver` on the `RentalDriver` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "public"."RentalDriver" DROP COLUMN "primaryDriver";
